<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
      <title>Drafts on Ryan Clarke </title>
      <generator uri="https://hugo.spf13.com">Hugo</generator>
    <link>http://ryanclarke.github.io/draft/index.xml/</link>
    <language>en-us</language>
    <author>Ryan Clarke</author>
    <copyright>© Ryan Clarke, 2014</copyright>
    <updated>Wed, 10 Sep 2014 13:46:31 EDT</updated>
    
    <item>
      <title>XAML to image</title>
      <link>http://ryanclarke.github.io/draft/XAML-to-image/</link>
      <pubDate>Wed, 10 Sep 2014 13:46:31 EDT</pubDate>
      <author>Ryan Clarke</author>
      <guid>http://ryanclarke.github.io/draft/XAML-to-image/</guid>
      <description>&lt;p&gt;I added a live tile to the Windows Phone app I&amp;rsquo;m building. That may not sound like much since almost every app has a live tile, but it&amp;rsquo;s not as straight-forward as I thought.&lt;/p&gt;

&lt;p&gt;Windows Phone 8 tiles will display whatever images you specify. I wanted a simple tile so I used the &lt;code&gt;FlipTileData&lt;/code&gt; with the &lt;code&gt;BackgroundImage&lt;/code&gt; set to my image URI.&lt;/p&gt;

&lt;p&gt;Creating an image out of xaml is not too hard either. Your tile xaml should be a &lt;code&gt;UserControl&lt;/code&gt;, which works really well with &lt;code&gt;WriteableBitmap&lt;/code&gt;&amp;rsquo;s &lt;code&gt;Render&lt;/code&gt; method which takes a &lt;code&gt;UIElement&lt;/code&gt; as it&amp;rsquo;s first parameter. Then you call &lt;code&gt;SaveJpeg&lt;/code&gt; on the bitmap and you&amp;rsquo;ve got an image.&lt;/p&gt;

&lt;p&gt;The plot just outlined is simple, but there are a number of little details which you must get right or your image will not look right. In fact, when I left them out in my original attempts at this tile it seemed like Windows Phone didn&amp;rsquo;t understand basic xaml controls, like &lt;code&gt;Grid&lt;/code&gt; and &lt;code&gt;StackPanel&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;Before we start you need to get the tile dimensions right. I&amp;rsquo;ll be showing a normal/medium sized square tile, so it&amp;rsquo;s just 336 by 336 pixels. Wide tiles are 336 by 691, and the tiny tiles can&amp;rsquo;t be live. You will be putting this width and height as properties on your tile control&amp;rsquo;s root xaml element. You will also be using them a lot of times in the code.&lt;/p&gt;

&lt;p&gt;So we have the tile control newed up and we have the dimensions.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;var tileControl = new MyTileControl();
var tileWidth = 336;
var tileHeight = 336;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Then we need to tell the tile what size it should be, since the properties on the root xaml element don&amp;rsquo;t fully do the job.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;tileControl.Measure(new Size(tileWidth, tileHeight));
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Here&amp;rsquo;s the part I was missing. If you leave this out the framework won&amp;rsquo;t know what size we just set and will just assume that the tile has no size and will pile everything up into the top corner regardless of grids or anything else. This one takes a &lt;code&gt;Rect&lt;/code&gt; (the first two parameters are the offset, so give it zeros).&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;tileControl.Arrange(new Rect(0, 0, tileWidth, tileHeight));
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Now can create the bitmap like I mentioned above. Of course, it needs to know the size too.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;var bitmap = new WritableBitmap(tileWidth, tile Height));
bitmap.Render(this, null); // Don&#39;t need a transform
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Don&amp;rsquo;t forget to &lt;code&gt;Invalidate&lt;/code&gt; the bitmap. This is just a scary way to say, please recalculate the entire thing just one more time just to make sure everything isn&amp;rsquo;t bonkers. It&amp;rsquo;s a very important step, but oddly, it doesn&amp;rsquo;t need to know the size again!&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;bitmap.Invalidate();
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Then you just call &lt;code&gt;SaveJpeg&lt;/code&gt; on the bitmap. It&amp;rsquo;s not particularly interesting, it&amp;rsquo;s mostly file-related stuff anyway, but I&amp;rsquo;ll include the code here just for completeness.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;var filePath = &amp;quot;/Shared/ShellContent/MediumTile.jpg&amp;quot;;

try
{
    using (var myStore = IsolatedStorageFile.GetUserStoreForApplication())
    using (var fileStream = new IsolatedStorageFileStream(filePath, FileMode.Create, myStore))
    {
        var tileQuality = 80; // 100 is best; don&#39;t go below 75
        // 0 is orientation: reserved for possible future use?!
        bitmap.SaveJpeg(fileStream, tileWidth, tileHeight, 0, tileQuality);
    }
}
catch (Exception ex)
{
    // do something
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;That&amp;rsquo;s pretty much it. Of course, there are a number of other things to make the code not crash in normal use. I double check that the jpeg write succeeded, I verify that the uri I give to the tile is valid. I have try-catches around all the dangerous code. I generate jpeg paths using a guid so I ensure the image gets recognized as new (and I delete all the old tile images to keep from using up space).&lt;/p&gt;

&lt;p&gt;I hope this quick run-through helps you know all the different ways you have to set the size in the tile-making process.&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>